import { Button } from "@/components/ui/button"

/**
 * Home page React component that renders a Button with the label "Click me".
 *
 * @returns The JSX element for the home page containing a single Button labeled "Click me".
 */
export default function Home() {
  return (
    <div>
      <Button>Click me</Button>
    </div>
  )
}